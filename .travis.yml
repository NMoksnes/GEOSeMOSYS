language: python
version: ~> 1.0
python:
  - "3.7"      # current default Python on Travis CI
  - "3.8"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  python -m unittest tests.test_file_present