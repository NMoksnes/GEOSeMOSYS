language: python
python:
  - "3.4"
  - "3.5"
  - "3.7"      # current default Python on Travis CI
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
python -m unittest test/test_file_present.ImportTestCase
